<%= form_with(model: subcategory) do |form| %>
  <% if subcategory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>
      <ul>
        <% subcategory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :subcategory_topic, style: "display: block" %>
    <%= form.text_field :subcategory_topic %>
  </div>
  <div>
    <%= form.label :subcategory_description, style: "display: block" %>
    <%= form.text_field :subcategory_description %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
