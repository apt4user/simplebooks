<%= form_with(model: carts) do |form| %>
  <% if cart.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(carts.errors.count, "error") %> prohibited this cart from being saved:</h2>
      <ul>
        <% carts.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.submit %>
  </div>
<% end %>
